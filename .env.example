# Redis
REDIS_PASSWORD=CHANGE_ME_TO_A_STRONG_PASSWORD
REDIS_ADDR=db.spb.arpa:6379

# Comma-separated CIDR ranges of trusted reverse proxies (e.g. Traefik)
# Only requests from these IPs will have X-Real-Ip / X-Forwarded-For headers trusted
TRUSTED_PROXIES=172.16.0.0/12,10.0.0.0/8

# For production: configure Redis TLS by setting up certificates and enabling
# TLS in both the Redis server and the Go application (go-redis TLSConfig option)
